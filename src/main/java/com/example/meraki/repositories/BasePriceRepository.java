package com.example.meraki.repositories;

import com.example.meraki.entities.BasePrice;
import org.springframework.stereotype.Repository;

@Repository
public interface BasePriceRepository extends BaseRepository<BasePrice>{
}
